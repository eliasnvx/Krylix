name: 🔨 Build

on:
  pull_request:
    branches: [ '26.1-dev', '1.21.1-dev', '1.20.1-dev', 'dev' ]
  push:
    branches: [ '26.1-dev', '1.21.1-dev', '1.20.1-dev', 'dev' ]
    paths-ignore:
      - '**.md'
      - '.gitignore'

jobs:
  build-mod:
    name: 🏗️ Build Mod (NeoForge + Fabric + Forge)
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: ✅ Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v3
      
      - name: ☕ Set up JDK 25
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '25'
          cache: 'gradle'
      
      - name: 🗂️ Setup Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-
      
      - name: 🧹 Cleanup Gradle cache
        run: |
          rm -f ~/.gradle/caches/modules-2/modules-2.lock
          rm -f ~/.gradle/caches/modules-2/gc.properties
      
      - name: 🔧 Grant execute permission for gradlew
        run: chmod +x gradlew
      
      - name: 🏗️ Build with Gradle
        run: ./gradlew build --no-daemon --stacktrace
        env:
          GRADLE_OPTS: "-Xmx3G"
      
      - name: 📊 Upload NeoForge artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: Krylix-NeoForge-${{ github.sha }}
          path: neoforge/build/libs/*.jar
          retention-days: 30
          
      - name: 📊 Upload Fabric artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: Krylix-Fabric-${{ github.sha }}
          path: fabric/build/libs/*.jar
          retention-days: 30
      
      - name: 📊 Upload Forge artifacts
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: Krylix-Forge-${{ github.sha }}
          path: forge/build/libs/*.jar
          retention-days: 30
      
      - name: 📝 Build summary
        if: success()
        run: |
          echo "### ✅ Build Successful!" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📦 NeoForge, Fabric, and Forge artifacts uploaded successfully!" >> $GITHUB_STEP_SUMMARY
